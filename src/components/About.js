import * as React from "react"

const About = ({fn}) => {
    return(
        <div className="xx">
            <p className="text"  onClick={fn}>About</p>
        </div>
    )
}

export default About;