import * as React from "react"

const Projects = ({fn}) => {
    return(
        <div className="xx">
            <p className="text"  onClick={fn}>Projects</p>
        </div>
    )
}

export default Projects;